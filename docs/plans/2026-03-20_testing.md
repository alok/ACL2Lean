# Testing Plan

**Date:** 2026-03-20

## Approach

Use Lean's `#guard` for compile-time tests (build failure = test failure).

## Areas to cover

- **Translator**: round-trip tests for each handler (let, case, quote, cond, list, cddr, evens/odds termination, multi-key case, local→private)
- **Parser**: nil→SExpr.nil, t→SExpr.t, empty list, keywords, qualified symbols, edge cases
- **Logic primitives**: `#eval` smoke tests for sorting functions on concrete inputs (isort, perm, how_many, etc.)
- **Lexorder**: ordering properties on concrete values

## Open questions

- Should translated-file compilation count as an integration test? (Currently verified by `translate-book.sh --verify`)
- Do we want property-based testing (e.g. `SlimCheck`) for Logic/Lexorder?
